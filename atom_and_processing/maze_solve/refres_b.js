function refresh_boundaries(){
        // for(var i=0;i<cols;++i){
        //   grid[index(0,i)].walls[0]=true;
        //  grid[index(rows-1,i)].walls[2]=true;
        // }
        // for(var i=0;i<rows;++i){
        //    grid[index(i,0)].walls[3]=true;
        //        grid[index(i,cols-1)].walls[1]=true;
        // }




}
